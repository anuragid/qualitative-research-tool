line-length = 120
target-version = "py311"
extend-exclude = ["alembic/"]

[lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[lint.per-file-ignores]
"app/main.py" = ["E402"]  # routers imported after app creation (intentional)

[lint.isort]
known-first-party = ["app"]
