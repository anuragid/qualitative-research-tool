# Known schema drift — items here are TOLERATED by check_migration_drift.py.
#
# THIS FILE MUST STAY EMPTY (no drift keys below this header).
#
# As of PR "fix(db): add missing indexes + reconcile model/schema drift" the
# original 12 tolerated items were ALL resolved:
#   - 7 missing indexes were created (CONCURRENTLY) by migration b8c4d2e3f5a6.
#   - video_analyses.step_status JSON->JSONB by migration a7b3c9d1e2f4.
#   - projects.status NOT NULL, users.email NOT NULL, and the
#     users_email_key / users_username_key UNIQUE constraints were reconciled
#     by tightening the MODEL (app/models/database_models.py) to match the DB —
#     no DDL, because the database was already correct.
#
# The drift gate (check_migration_drift.py) now requires the autogenerate diff
# to be EMPTY. Any NEW entry added here is a regression: fix the migration or
# the model instead of re-tolerating drift.
#
# Format (for reference only — there should be NO active entries): one
# canonical drift key per line. Lines starting with '#' and blank lines are
# ignored. Keys are derived from alembic autogenerate diff tuples by
# check_migration_drift.py:
#   add_index:<index_name>
#   modify_nullable:<table>:<column>      (direction-agnostic)
#   modify_type:<table>:<column>          (direction-agnostic)
#   remove_constraint:<table>:<constraint_name>
